[package]
name = "rootspace"
version = "0.1.0"
authors = ["Eleanore Young"]

[workspace]

[features]
diagnostics = ["game/diagnostics", "engine/diagnostics"]

[dependencies]
clap = {version = "^2.0.0", default-features = false}
log = "^0.4.1"
failure = "^0.1.1"
fern = "^0.5.5"
engine = {path = "engine"}
game = {path = "game"}

[dev-dependencies]
assertions = {git = "https://gitlab.com/nausicaea/assertions.git"}
